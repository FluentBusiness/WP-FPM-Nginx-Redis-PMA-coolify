# 🚀 High-Performance WordPress Docker Stack (Coolify Ready)

Этот репозиторий содержит конфигурацию для развертывания высокопроизводительного стека WordPress с использованием Docker Compose. Сборка оптимизирована для работы под управлением **Coolify**, включает в себя Nginx FastCGI Cache, Redis Object Cache, PHP 8.5 с JIT и автоматическую установку плагинов экосистемы Fluent/Elementor.

## ✨ Особенности стека

* **App Server:** WordPress на базе PHP 8.5-FPM (Custom Build).
* Включен **JIT** (Just In Time Compiler) и оптимизированный **Opcache**.
* Установлены расширения: `pdo_mysql`, `zip`, `redis`.
* Встроенный **WP-CLI** для автоматизации.


* **Web Server & Caching:** Nginx (Latest).
* Настроен **FastCGI Cache** (кэширование страниц на уровне сервера).
* Умная очистка кэша (совместимость с Nginx Helper).
* Поддержка **CompressX** (раздача WebP/AVIF без запуска PHP).
* Защита от XMLRPC-атак и доступа к скрытым файлам.


* **Database:** MariaDB 11.
* **Object Cache:** Redis (хранение сессий и объектного кэша WP).
* **Management:** phpMyAdmin (для управления БД).
* **Automation:** Скрипт инициализации (`init-script.sh`) автоматически:
* Устанавливает набор плагинов (Fluent CRM, Elementor, SEOPress и др.).
* Настраивает `wp-config.php`.
* Включает Redis Object Cache.



---

## 🌐 Настройка доменов и DNS (Важно!)

Перед развертыванием стека необходимо направить основной домен и поддомен для управления базой данных на IP-адрес вашего сервера. Это критически важно для того, чтобы Coolify смог автоматически сгенерировать SSL-сертификаты (Let's Encrypt) при первом запуске.

### 1. Получение IP-адреса
Используйте IP-адрес вашего VPS сервера (он был выведен в конце работы скрипта `setup.sh` или доступен в панели хостинга).

### 2. Создание DNS-записей
Зайдите в панель управления DNS вашего регистратора домена и создайте две **A-записи**:

| Тип записи | Имя (Host) | Значение (Value) | Назначение |
| :--- | :--- | :--- | :--- |
| **A** | `@` (или ваше доменное имя) | `ВАШ_IP_СЕРВЕРА` | Основной сайт WordPress |
| **A** | `db` (или `pma`, `sql`) | `ВАШ_IP_СЕРВЕРА` | Доступ к phpMyAdmin |

> **☁️ Для пользователей Cloudflare:**
> При первичной настройке рекомендуется отключить проксирование (переключить облако с оранжевого на серое: **DNS Only**). Это гарантирует успешный выпуск SSL-сертификата. После успешного деплоя можно включить проксирование обратно.

### 3. Указание доменов в Coolify
При настройке сервиса в Coolify не забудьте указать эти домены:
* Для сервиса **wordpress**: Укажите основной домен (например, `https://example.com`).
* Для сервиса **phpmyadmin**: Зайдите в настройки контейнера `phpmyadmin` внутри стека и укажите поддомен (например, `https://db.example.com`).

## 🛠 Предварительная настройка сервера (VPS)

Перед установкой Coolify рекомендуется подготовить "чистый" сервер с помощью скрипта `setup.sh`.

**⚠️ ВНИМАНИЕ:** Этот скрипт отключает вход по паролю и настраивает доступ только по SSH-ключам.

1. Зайдите на сервер под `root`.
2. Создайте файл и вставьте содержимое `setup.sh`:
```bash
nano setup.sh
# Вставьте код, сохраните (Ctrl+O, Enter) и выйдите (Ctrl+X)
chmod +x setup.sh
./setup.sh

```


3. **Что сделает скрипт:**
* Обновит систему.
* Создаст **Swap 2GB** (критично для сборки Docker-образов на слабых VPS).
* Сгенерирует SSH-ключи (OpenSSH и PPK).
* Настроит UFW (Firewall), открыв только порты 22, 80, 443.
* Установит Docker и Fail2Ban.
* **В конце выведет приватные ключи — ОБЯЗАТЕЛЬНО сохраните их, иначе потеряете доступ к серверу!**



---

## 📦 Структура проекта

```text
.
├── docker-compose.yaml    # Основной манифест сервисов
├── Dockerfile             # Сборка образа WP (PHP 8.5 + расширения)
├── nginx.conf             # Конфиг Nginx (Кэш, безопасность, сжатие)
├── wp-php.ini             # Тюнинг PHP (Memory Limit 512M, Uploads 256M, JIT)
├── init-script.sh         # Авто-установка плагинов и настройка WP при старте
├── setup.sh               # Скрипт первоначальной настройки VPS
└── .env                   # (Создается автоматически в Coolify)

```

---

## 🚀 Развертывание в Coolify

### 1. Создание сервиса

1. В Coolify создайте новый ресурс типа **Docker Compose**.
2. Скопируйте содержимое `docker-compose.yaml` в редактор конфигурации.

### 2. Переменные окружения (Environment Variables)

Coolify автоматически подставляет многие переменные, но убедитесь, что следующие значения определены в разделе "Environment Variables" вашего сервиса:

| Переменная | Описание | Значение по умолчанию / Пример |
| --- | --- | --- |
| `SERVICE_FQDN_WORDPRESS` | URL вашего сайта | `https://example.com` |
| `SERVICE_USER_WORDPRESS` | Пользователь БД | `wordpress` |
| `SERVICE_PASSWORD_WORDPRESS` | Пароль пользователя БД | *(генерируется Coolify)* |
| `SERVICE_PASSWORD_ROOT` | Root пароль БД | *(генерируется Coolify)* |

> **Примечание:** В `init-script.sh` есть заготовки под ключи S3 (Fluent Cloud Storage). Если вы планируете их использовать, добавьте соответствующие ENV (например, `FLUENT_BOARDS_CLOUD_STORAGE_ACCESS_KEY`) и пробросьте их в `docker-compose.yaml`.

### 3. Тома (Volumes)

Убедитесь, что Docker Compose подтягивает локальные файлы конфигурации. В Coolify, если вы используете Git-источник, файлы подтянутся сами. Если вы используете "Raw Docker Compose", вам может потребоваться создать файлы конфигов через интерфейс Coolify (раздел **Storages / Configs**) и смонтировать их:

* `nginx.conf` -> `/etc/nginx/conf.d/default.conf` (в сервисе nginx)
* `wp-php.ini` -> `/usr/local/etc/php/conf.d/wp-php.ini` (в сервисе wordpress)
* `init-script.sh` -> `/usr/local/bin/init-script.sh` (в сервисе wordpress)

### 4. Запуск

Нажмите **Deploy**.

1. Соберется образ WordPress (это займет время, так как компилируются расширения PHP).
2. Запустятся MariaDB и Redis.
3. При первом старте контейнера `wordpress` сработает `init-script.sh`.

---

## ⚙️ Технические детали настройки

### 1. Nginx FastCGI Cache

Кэширование настроено в `nginx.conf`.

* **Логика пропуска кэша:**
* Метод `POST`.
* URL с query-параметрами (кроме UTM-меток, если настроить дополнительно).
* Наличие cookies: `wordpress_logged_in`, `woocommerce_items_in_cart` и т.д.
* Специфические URL: `/wp-admin/`, `/xmlrpc.php`, `/wp-json/`.


* **Очистка кэша:**
* Папка кэша: `./nginx-cache` (общий том между Nginx и WordPress).
* Плагин **Nginx Helper** (устанавливается скриптом) должен быть настроен на метод очистки "Delete local files".



### 2. PHP Optimization (`wp-php.ini`)

* **Memory Limit:** 512M (для работы Elementor и тяжелых плагинов).
* **Opcache:** `opcache.validate_timestamps=1` и `revalidate_freq=2` (идеально для Docker: изменения файлов видны почти сразу, но производительность высокая).
* **JIT:** Включен в режиме `1255` с буфером 100M для ускорения вычислений.

### 3. Авто-установка плагинов (`init-script.sh`)

## 🧩 Установленные плагины

При первом запуске скрипт автоматизации (`init-script.sh`) устанавливает и активирует следующий набор плагинов. Сборка ориентирована на экосистему **Fluent** и высокую производительность.

### 🚀 Экосистема Fluent (Бизнес-инструменты)
Полный набор инструментов для автоматизации бизнеса внутри WordPress:
* **Fluent CRM** (`fluent-crm`) — Полноценная CRM-система и Email-маркетинг (автоматизация рассылок, управление контактами).
* **Fluent Forms** (`fluentform`) — Продвинутый конструктор форм (контактные формы, опросы, лидогенерация).
* **Fluent Support** (`fluent-support`) — Система технической поддержки (Helpdesk/Тикеты) прямо в админке.
* **Fluent Boards** (`fluent-boards`) — Управление проектами и задачами (аналог Trello/Jira).
* **Fluent Community** (`fluent-community`) — Создание сообщества, групп и обсуждений на сайте (аналог Circle/BuddyBoss).
* **Fluent Booking** (`fluent-booking`) — Система бронирования времени и встреч (аналог Calendly).
* **Fluent SMTP** (`fluent-smtp`) — Обеспечивает надежную доставку писем через внешние SMTP-сервисы.
* **Fluent Snippets** (`fluent-snippets`) — Безопасное добавление кастомного PHP/JS/CSS кода без редактирования файлов темы.
* **Fluent Affiliate** (`fluent-affiliate`) — Организация собственной партнерской (реферальной) программы.
* **Fluent Cart** (`fluent-cart`) — Легкая корзина для прямых продаж товаров и услуг.
* **Fluent Security** (`fluent-security`) — Улучшение безопасности и логирование действий.

### 🎨 Дизайн и Контент (Page Builders)
* **Elementor** (`elementor`) — Самый популярный визуальный конструктор страниц.
* **Essential Addons for Elementor** (`essential-addons-for-elementor-lite`) — Набор дополнительных виджетов для Elementor.
* **Templately** (`templately`) — Облачная библиотека готовых шаблонов для Elementor и Gutenberg.
* **Essential Blocks** (`essential-blocks`) — Набор блоков для штатного редактора Gutenberg.
* **BetterDocs** (`betterdocs`) — Плагин для создания базы знаний (Wiki/Документации).

### ⚡ Производительность и Инфраструктура
Эти плагины настроены для работы с Docker-окружением (Redis, Nginx):
* **Redis Object Cache** (`redis-cache`) — Подключает WordPress к сервису Redis для кэширования запросов к базе данных (настраивается автоматически).
* **Nginx Helper** (`nginx-helper`) — Автоматически очищает FastCGI-кэш Nginx при обновлении контента (публикация постов, комментарии).
* **CompressX** (`compressx`) — Оптимизация изображений. Генерирует WebP и AVIF форматы, которые Nginx отдает напрямую через спец. правила.

### 🛠 Утилиты и SEO
* **SEOPress** (`seopress`) — Мощный SEO-комбайн (мета-теги, sitemap, социальные сети) без рекламы в интерфейсе.
* **WPvivid Backup** (`wpvivid-backuprestore`) — Создание резервных копий сайта и миграция.
* **Cyr-to-Lat** (`cyr-to-lat`) — Автоматическая транслитерация кириллических заголовков в латинские URL (SEO-friendly).
* **Loco Translate** (`loco-translate`) — Перевод тем и плагинов прямо из админки.
* **Really Simple SSL** (`really-simple-ssl`) — Обеспечивает корректную работу HTTPS и исправляет смешанный контент.
* **Paymattic** (`paymattic`) — Плагин для приема платежей и донатов (интеграция со Stripe/PayPal и др.).
* **Aimogen** (`aimogen`) — Инструмент для генерации контента с использованием ИИ.

---

## 🛡 Безопасность

1. **Скрытые файлы:** Nginx блокирует доступ к `.git`, `.env`, `.ini`, `.log`.
2. **Запрет PHP в Uploads:** Выполнение скриптов в `/wp-content/uploads` запрещено на уровне Nginx.
3. **XML-RPC:** Доступ к `xmlrpc.php` закрыт (защита от DDoS и Brute-force).
4. **SSH Hardening:** (Через `setup.sh`) Отключен вход по паролю, изменен порт (опционально), настроен Fail2Ban.

---

## ❓ Troubleshooting

**Как проверить, работает ли кэш Nginx?**
Откройте инструменты разработчика в браузере (Network), обновите страницу. Ищите заголовок:
`X-FastCGI-Cache: HIT` (работает) или `MISS` (первый заход).

**Как сбросить статус "Настройка завершена"?**
Если нужно, чтобы `init-script.sh` отработал заново (например, для переустановки плагинов), выполните в консоли контейнера WordPress:

```bash
rm /var/www/html/.setup_done
# Перезапустите контейнер

```

**Доступ к phpMyAdmin**
Доступен на порту `8070` (или другом, указанном в Coolify).

* **Server:** `mariadb`
* **User:** `root`
* **Password:** (См. переменную `$SERVICE_PASSWORD_ROOT`)